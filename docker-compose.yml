version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:5000"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      AWS_REGION: ${AWS_REGION:-ap-northeast-2}
      # Redis 환경변수 모두 제거 (ElastiCache는 SSM 경유)